package com.mycompany.testetecnico2;

import java.util.Scanner;

/**
 *
 * @author rosealquino
 */
public class TesteTecnico2 {

    private static double T;

    public static void main(String[] args) {
        Scanner d = new Scanner(System.in);

double valor, valort, produto, desconto = 0;

System.out.println("Digite o produto: ");
System.out.println("Digite a quantidade: ");

valor = d.nextInt();
        double L = 0;
        double T = 0;
        int quantidade;

if (produto = L)&&(quantidade <= 2)

desconto = (valor*(3/100));

valort = desconto+valor;

else if (produto = L)&&(quantidade >= 5)

desconto = (valor*(5/100));

valort = desconto+valor;

else if (produto = T)&&(quantidade <= 2)

desconto = (valor*(4/100));

valort = desconto+valor;

else if (produto = T)&&(quantidade >= 5)

desconto = (valor*(6/100));

valort = desconto+valor;

System.out.print("Total do desconto : " +desconto);

System.out.print("Total com desconto : " +valort);

System.out.print("...Fim do Programa...");

}

}


